<!DOCTYPE html>
<html>
<head>
  <title>Edit Idea</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-900">
  <div class="max-w-xl mx-auto mt-10 p-6 bg-white rounded shadow">
    <h2 class="text-xl font-bold mb-4">Edit Your Idea</h2>
    <form action="/edit-idea/<%= idea._id %>" method="POST" class="space-y-4">
      <input type="text" name="sector" value="<%= idea.sector %>" class="w-full border px-3 py-2 rounded" required />
      <input type="text" name="problem" value="<%= idea.problem %>" class="w-full border px-3 py-2 rounded" required />
      <textarea name="description" rows="4" class="w-full border px-3 py-2 rounded"><%= idea.description %></textarea>
      <div class="flex justify-end">
        <button class="bg-blue-600 text-white px-4 py-2 rounded">Update</button>
      </div>
    </form>
    <form action="/edit-profile" method="POST" class="space-y-4">
  <input type="text" name="name" value="<%= user.name %>" required />
  <input type="email" name="email" value="<%= user.email %>" required />
  <textarea name="bio"><%= user.bio %></textarea>
  <button type="submit">Save</button>
</form>
  </div>
</body>
</html>
